// Re-exports the Prisma-generated type instead of hand-maintaining a
// duplicate interface, so the entity shape can never drift from the schema.
export type { CollectedSticker } from '@prisma/client';
