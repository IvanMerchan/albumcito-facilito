import type { Config } from "tailwindcss";

// Tailwind CSS v4 is CSS-first: theme tokens live in `app/globals.css`
// via `@theme`, and content sources are auto-detected. This file is kept
// as the single place to add JS-side config (plugins, safelist, etc.)
// if the project ever needs it.
const config: Config = {};

export default config;
